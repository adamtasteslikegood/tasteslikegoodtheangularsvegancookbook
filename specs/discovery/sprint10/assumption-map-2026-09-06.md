# Sprint 10 assumption map (2026-09-06)

Product-discovery pass over `ost.json` (linter exit 0, 8 opportunities) and the locked decisions in `product-grill-2026-09-06.md`. Inputs and reruns:

```bash
P="${PRODUCT_SKILLS_DIR:?Set PRODUCT_SKILLS_DIR to the installed product-skills skills/ directory}"
python3 "$P/product-discovery/scripts/assumption_mapper.py" specs/discovery/sprint10/assumptions.csv --top 25
python3 "$P/product-skills/scripts/discovery_cadence_tracker.py" --input specs/discovery/sprint10/discovery_log.json --as-of 2026-09-06 --output human
```

Scoring: `priority = risk × (1 − certainty)`; high-risk, low-certainty first (Torres / Bland assumption mapping). Risk and certainty are the agent's estimates from the evidence column in `assumptions.csv`, not measurements.

## Ranked test plan (21 assumptions)

| Rank | Score | Category     | Assumption (OST ref)                                                                         | Test the mapper suggests            | What Sprint 10 actually does                                                                                                                                                                         |
| ---- | ----- | ------------ | -------------------------------------------------------------------------------------------- | ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | 0.56  | desirability | Mobile visitors who land on `/r/<slug>` will save at a measurable rate (outcome)             | problem interviews / fake door      | **Untestable pre-launch by Adam's decision** (no external users until the done-bar). Sprint 10 ships the instrumentation so the first users are measured; the test runs when they arrive.            |
| 2    | 0.56  | desirability | A non-author uses the settings tab within 4 weeks of first users (opp7)                      | problem interviews / fake door      | Rank 9 in the sprint, first cut. Fake-door is cheap: a "Settings" header entry that opens the four options can log clicks via RUM before the model picker exists.                                    |
| 3    | 0.36  | feasibility  | FCP on `/` ≤ 2.0 s via code split, deferred modals, no render-blocking font (opp2.sol2)      | technical spike                     | **Half-day spike first**: measure how much of the ~1 MB shell is removable before committing the layout item's exit number.                                                                          |
| 4    | 0.35  | desirability | Enough users opt into RUM for the five inputs to have usable data (opp5)                                  | problem interviews / fake door      | Unknown until users exist. Declining must stop RUM entirely; verify zero RUM requests after decline and estimate usable volume only from consented sessions.                                                                         |
| 5    | 0.35  | feasibility  | WebP variants + srcset + `fetchpriority` land LCP ≤ 2.5 s on `/r` and `/browse` (opp3.sol1)  | technical spike                     | **Spike on one recipe on the local loop before the pipeline**: hand-resize one hero, measure LCP. If < 2.5 s, the variant pipeline is the SI; if not, the SPA-shell and Valkey items own the number. |
| 6    | 0.30  | viability    | Recipes kept per week is the right North Star                                                | willingness-to-pay (mapper default) | Not a pricing question. Revisit after four weeks of RUM data; Amplitude's test is whether the inputs move it.                                                                                        |
| 7    | 0.30  | viability    | Filtered/sorted `/browse` does not create duplicate-content indexing (opp4.sol2)             | (mapper default)                    | Design rule before code: filtered views get `noindex` or a canonical to the unfiltered page; run the existing SEO canonical gate.                                                                    |
| 8    | 0.25  | feasibility  | Express-side sharp resize proxy p95 < 300 ms under 1 Gi (opp3.sol3)                          | technical spike                     | Only if rank 5's spike fails on the pipeline path.                                                                                                                                                   |
| 9    | 0.25  | feasibility  | A pushed PR yields a staging preview URL within 10 min (opp6.sol1)                           | technical spike                     | Adam decided the Cloud Deploy approach; the first PR through it is the test.                                                                                                                         |
| 10   | 0.25  | feasibility  | Every offered model is on the GA allowlist and passes 12/12 golden-set prompts (opp7.sol1)   | technical spike                     | Eval spec is in the PRD; the golden set is written before the picker UI.                                                                                                                             |
| 11   | 0.20  | feasibility  | Dropping the Valkey image read-through alone gets p95 < 2 s (opp3.child.sol2)                | technical spike                     | Alternative to KAN-268's fix; measured on staging preview with a flag if the fix slips.                                                                                                              |
| 12   | 0.18  | usability    | First-time visitor finds public recipes within 30 s with Browse in the header (opp1.sol1)    | moderated usability test            | **Already failed once without the header link (P1) and reproduced by the agent walkthrough.** After nav ships: repeat with P1 plus two people, 2 of 3 within 30 s.                                   |
| 13   | 0.16  | feasibility  | Variants stay cache-busted by `?v=` and the 24 h `Cache-Control` (opp3.sol1)                 | technical spike                     | curl headers before/after regenerate; part of the variant SI's acceptance.                                                                                                                           |
| 14   | 0.16  | usability    | "Find the dessert recipes" ≤ 30 s in a 134-recipe Kitchen (opp4.sol1)                        | moderated usability test            | Adam's own Kitchen is the test bed; run before and after filter/sort.                                                                                                                                |
| 15   | 0.16  | feasibility  | Datadog RUM exposes the five inputs in a week; consent-decline stops all traffic (opp5.sol1) | technical spike                     | Acceptance of the instrumentation SI.                                                                                                                                                                |
| 16   | 0.15  | feasibility  | Expiry-aware refresh + retry-once gets redis errors < 1%, image p95 < 2 s (KAN-268)          | technical spike                     | Root cause is evidenced; this is the sprint's rank-1 item, acceptance on RCP-98.                                                                                                                     |
| 17   | 0.12  | usability    | Empty-Kitchen CTA gets guests to `/browse` without scrolling (opp1.sol2)                     | moderated usability test            | gstack check: CTA above the fold at 390×844.                                                                                                                                                         |
| 18   | 0.09  | usability    | LQIP paints before 1 s with CLS 0 (opp3.sol2)                                                | moderated usability test            | Lighthouse filmstrip + CLS.                                                                                                                                                                          |
| 19   | 0.09  | viability    | Real-anchor header nav is crawlable and lifts search discovery (opp1.sol1)                   | (mapper default)                    | Snapshot: header items are `<a href>`; Search Console after four weeks.                                                                                                                              |
| 20   | 0.06  | usability    | ≥ 4 cards above the fold at 390×844 with 2-up cards (opp2.sol1)                              | moderated usability test            | gstack bounding-box count; deterministic.                                                                                                                                                            |
| 21   | 0.04  | usability    | Identical footer links across the four surfaces (opp1.sol3)                                  | moderated usability test            | Snapshot diff; deterministic.                                                                                                                                                                        |

## Cadence tracker

`discovery_cadence_tracker.py` **refused, exit 5: fewer than 2 dated interviews.** The log holds one non-author participant (P1, phone, task failed) whose date was not recorded, one agent walkthrough (not a user), and the author's continuous self-observation (not an interview). Nothing was fabricated to make it score. The habit does not exist yet, which is consistent with the locked decision that Sprint 10 is pre-launch and evidence for the obvious defects is lab and inspection. First fix: record P1's date; second: the two post-nav walkthroughs in rank 12 become interviews 2 and 3.

## Discovery-sprint plan folded into Sprint 10 (skill's 10-day shape, compressed)

- **Day 1:** charter (KAN-269): epic, acceptance rows, KAN rows. Spikes for ranks 3 and 5 on the local loop (half day each). Record P1's date.
- **Days 2–4:** KAN-268 fix, instrumentation with consent, image variants or the interim proxy depending on the rank-5 spike.
- **Days 5–7:** nav + empty-state CTA + footer; mobile layout; filter/sort in the Kitchen; `/browse` sort with the canonical rule.
- **Day 8:** post-nav walkthroughs (P1 + 2), Lighthouse on the staging preview URL for all three surfaces.
- **Day 9:** evidence synthesis against the exit number (LCP ≤ 2.5 s ×3) and the walkthrough pass (2 of 3).
- **Day 10:** decision: proceed to Adam's done-bar review, pivot (image pipeline vs proxy; filter scope), or stop an item. Settings tab ships only if everything above is green.

## Sample size and MDE

None computable: the outcome metric has no baseline and no traffic (pre-launch by decision). The lab exit number (LCP) needs no sample; the usability tests use Nielsen's 5-user heuristic scaled to the available pool (3), which finds most navigation failures but proves nothing statistical. State this in the sprint plan rather than inventing an MDE.
