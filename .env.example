# Copy to .env or .env.local and fill in your key.
# Only VITE_ prefixed vars are exposed to client code by Vite-based dev servers.
VITE_GEMINI_API_KEY=your_gemini_api_key_here
# Optional fallback supported by the app
VITE_API_KEY=your_gemini_api_key_here
