**everything syncs. across multiple browsers on different devices logged in at the same time.**

- [x] Image generation triggered on import of recipes from old app/image links
- [x] image generation stays with recipe that initiated call too `generate image``, even when switching views/modals mid-generation.

**new feat. upgrades**

- [ ] previous TODOs - ai images are high quality, allow up too 5 images per recipe so if user regenerates image the image frame becomes a gallery. If user wants to remove an image or replace/regenerate after reaching the 5 image limits, instead of a `delete image` option, removal of image through `report issue/remove image` action that asks user to select a reason for removing/replacing image with a few mulit-choice options and a comments/other that requires user to select or explain a reason for removing an image that helps identify errors in the gen-ai process and discourages frivolous image generation. **only users logged in with their google accounts should have access to multiple image features ** guests should see the same UX as exists now. The report a problem link should be somewhere that users can find, but out of the way as too discourage use other then for constructive feedback..
- [ ] This seems like a point where the next upgrade will focus on rate-limiting feature use of existing working features and feedback.
- [ ] Finally pressing `back` button or refresh brings user back to home page, unless they just generated a recipe and hit `generate` button, that bring s them back too a text box that has the last requested generation text still present.
- [ ] when in the `add to cookbook` modal, creating a new cookbook adds too the list, but does not select the new cookbook as a choice for adding recipe too.
- [ ] Profile window/modal should show user e-mail with asterisks i.e `ad******n3@gmal.com` or whatever is most conventional.
      General note: any UI/UX feature or styles - when there is a question on how something should look/feel: go with what major platforms/ things user would expect or be intuitive.. Everything implemented so far is great no change unless specified in the previous list.

Last though:
**\*Basic SEO**

- Almost forgot: site should be visible to search engines and encourage listing on google. If possible submit too major search engine or provide instuctions/add SEO tags/meta-data for obvious categories. .....(vegan, recipes, cookbook, health food, tasteslikegood, good tastes, vegan \_\_\_\_{all that applies}.. cooking/recipe all that applies.

overall, everything seems to be working, my profile has over 50 and counting recipes with images, auth is fast, working across session (what do big sites do about limiting active / timeouts?) Great work! feedback should send e-mail too issues or comments or feedback @tatestlikegood.org, if possible include subject with type of comment ie. feedback vs. Issue/offensive content/images.. )

**Next steps**

- work on ways to prevent/ identify obvious abuse/misuse of features
